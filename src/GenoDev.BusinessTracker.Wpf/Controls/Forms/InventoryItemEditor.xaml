<UserControl x:Class="GenoDev.BusinessTracker.Wpf.Controls.InventoryItemEditor"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:controls="clr-namespace:GenoDev.BusinessTracker.Wpf.Controls">
    <StackPanel Width="500">
        <TextBlock Text="Nazwa (wymagane):" Margin="0,0,0,5" />
        <TextBox Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,10" />

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="10" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <StackPanel Grid.Column="0">
                <TextBlock Text="EAN:" Margin="0,0,0,5" />
                <TextBox Text="{Binding Ean, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,10" />
            </StackPanel>
            <StackPanel Grid.Column="2">
                <TextBlock Text="Kod producenta:" Margin="0,0,0,5" />
                <TextBox Text="{Binding ManufacturerCode, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,10" />
            </StackPanel>
        </Grid>

        <TextBlock Text="Jednostka:" Margin="0,0,0,5" />
        <TextBox Text="{Binding Unit, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,10" />

        <TextBlock Text="Opis:" Margin="0,0,0,5" />
        <controls:ResizableTextBox Text="{Binding Description, UpdateSourceTrigger=PropertyChanged}"
                                  AcceptsReturn="True"
                                  MinHeight="80"
                                  TextWrapping="Wrap"
                                  VerticalScrollBarVisibility="Auto"
                                  Margin="0,0,0,20" />

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Anuluj" Command="{Binding CancelCommand}" Margin="0,0,10,0" Padding="15,5" />
            <Button Content="Zapisz" Command="{Binding SaveCommand}" Padding="15,5"
                    Background="{DynamicResource PrimaryButtonBackgroundBrush}"
                    Foreground="{DynamicResource PrimaryButtonForegroundBrush}"
                    IsDefault="True" />
        </StackPanel>
    </StackPanel>
</UserControl>
