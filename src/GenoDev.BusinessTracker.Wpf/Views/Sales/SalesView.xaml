<UserControl x:Class="GenoDev.BusinessTracker.Wpf.Views.Sales.SalesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:GenoDev.BusinessTracker.Wpf.Views.Sales"
             xmlns:controls="clr-namespace:GenoDev.BusinessTracker.Wpf.Controls"
             xmlns:vm="clr-namespace:GenoDev.BusinessTracker.Wpf.ViewModels.Sales"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             d:DataContext="{d:DesignInstance vm:SalesViewModel}">
    <UserControl.Resources>
        <DataTemplate DataType="{x:Type vm:OrdersViewModel}">
            <local:OrdersView />
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:MailingViewModel}">
            <local:MailingView />
        </DataTemplate>
    </UserControl.Resources>
    <Grid>
        <TabControl TabStripPlacement="Left"
                    SelectedIndex="{Binding SelectedTabIndex}"
                    Style="{StaticResource SideNavigationTabControlStyle}">
            <TabItem>
                <TabItem.Header>
                    <controls:SideNavigationHeader Icon="{StaticResource OrdersNavigationGeometry}"
                                                   Text="Zamów." />
                </TabItem.Header>
                <ContentControl Content="{Binding OrdersViewModel}" />
            </TabItem>
            <TabItem>
                <TabItem.Header>
                    <controls:SideNavigationHeader Icon="{StaticResource MailingNavigationGeometry}"
                                                   Text="Mailing" />
                </TabItem.Header>
                <ContentControl Content="{Binding MailingViewModel}" />
            </TabItem>
        </TabControl>
    </Grid>
</UserControl>
