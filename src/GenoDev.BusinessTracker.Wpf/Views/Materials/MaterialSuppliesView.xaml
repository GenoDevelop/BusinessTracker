<UserControl x:Class="GenoDev.BusinessTracker.Wpf.Views.Materials.MaterialSuppliesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:vm="clr-namespace:GenoDev.BusinessTracker.Wpf.ViewModels.Materials"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             d:DataContext="{d:DesignInstance vm:MaterialSuppliesViewModel}">
    <Grid Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="180"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Grid Grid.Column="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Toolbar -->
            <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                <TextBlock Text="Dostawy" FontSize="18" FontWeight="Bold" VerticalAlignment="Center" Margin="0,0,10,0" />
                <Button Command="{Binding CreateSupplyCommand}" Style="{StaticResource IconButton}" ToolTip="Dodaj dostawę" Margin="0,0,5,0">
                    <Path Data="M19,13H13V19H11V13H5V11H11V5H13V11H19V13Z" Fill="Green" Stretch="Uniform" Width="18" Height="18" />
                </Button>
                <Button Command="{Binding RefreshCommand}" Style="{StaticResource IconButton}" ToolTip="Odśwież">
                    <Path Data="M17.65,6.35C16.2,4.9 14.21,4 12,4A8,8 0 0,0 4,12A8,8 0 0,0 12,20C15.73,20 18.84,17.45 19.73,14H17.65C16.83,16.33 14.61,18 12,18A6,6 0 0,1 6,12A6,6 0 0,1 12,6C13.66,6 15.14,6.69 16.22,7.78L13,11H20V4L17.65,6.35Z" Fill="DodgerBlue" Stretch="Uniform" Width="18" Height="18" />
                </Button>
            </StackPanel>

            <!-- Date Range Picker (Single Widget) -->
            <Grid Grid.Row="1" Margin="0,0,0,10">
                <ToggleButton x:Name="DateRangeToggle" HorizontalContentAlignment="Stretch">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="{Binding StartDate, StringFormat=d}" />
                            <TextBlock Text=" - " Visibility="{Binding EndDate, Converter={StaticResource NullToAllConverter}, ConverterParameter='Visible;Collapsed'}" />
                            <TextBlock Text="{Binding EndDate, StringFormat=d}" />
                        </StackPanel>
                        <Path Grid.Column="1" Data="M7,10L12,15L17,10H7Z" Fill="Gray" VerticalAlignment="Center" Margin="5,0,0,0"/>
                    </Grid>
                </ToggleButton>
                <Popup IsOpen="{Binding IsChecked, ElementName=DateRangeToggle}" PlacementTarget="{Binding ElementName=DateRangeToggle}" StaysOpen="False">
                    <Border Background="White" BorderBrush="Gray" BorderThickness="1">
                        <Calendar SelectionMode="SingleRange" x:Name="RangeCalendar" />
                    </Border>
                </Popup>
            </Grid>

            <!-- Supplies List -->
            <ListBox Grid.Row="2" ItemsSource="{Binding Supplies}" Margin="0,0,0,10">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding}" Padding="5"/>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>

            <!-- Pagination -->
            <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Center">
                <Button Command="{Binding PreviousPageCommand}" Style="{StaticResource IconButton}" Margin="5,0">
                    <Path Data="M15.41,16.58L10.83,12L15.41,7.41L14,6L8,12L14,18L15.41,16.58Z" Fill="DodgerBlue" Stretch="Uniform" Width="16" Height="16" />
                </Button>
                <TextBlock VerticalAlignment="Center" Margin="5,0">
                    <Run Text="{Binding CurrentPage, Mode=OneWay}" />
                    <Run Text="/" />
                    <Run Text="{Binding TotalPages, Mode=OneWay}" />
                </TextBlock>
                <Button Command="{Binding NextPageCommand}" Style="{StaticResource IconButton}" Margin="5,0">
                    <Path Data="M8.59,16.58L13.17,12L8.59,7.41L10,6L16,12L10,18L8.59,16.58Z" Fill="DodgerBlue" Stretch="Uniform" Width="16" Height="16" />
                </Button>
            </StackPanel>
        </Grid>

        <!-- Right Panel (Empty for now) -->
        <Grid Grid.Column="1" Background="#F9F9F9" Margin="10,0,0,0">
            <TextBlock Text="Wybierz dostawę z listy, aby zobaczyć szczegóły" HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="Gray"/>
        </Grid>
    </Grid>
</UserControl>
